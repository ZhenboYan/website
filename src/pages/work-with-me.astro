---
import BaseLayout from '../layouts/BaseLayout.astro';
import SectionLabel from '../components/common/SectionLabel.astro';
import CTALink from '../components/common/CTALink.astro';
import IntakeForm from '../components/work/IntakeForm.astro';
---

<BaseLayout title="Work With Me — Catherine McMillan">
  <!-- Page Header -->
  <section class="page-header section">
    <div class="container">
      <SectionLabel text="Work With Me" />
      <h1 class="page-header__title">
        I partner with founders building in AI and professionals navigating what's next.
      </h1>
      <p class="page-header__subtext">
        I keep a few hours each week for introductory conversations. If there's a fit, I'll send over a time to connect.
      </p>
    </div>
  </section>

  <!-- Advisory -->
  <section class="service" id="advisory">
    <div class="service__inner container">
      <div class="service__text">
        <span class="service__label">For Startups</span>
        <h2 class="service__title">Strategic Advisory</h2>
        <p>
          I work with early-stage founders building products, platforms, and communities in and around AI. My advisory relationships are long-term and equity-based — not one-off calls.
        </p>
        <p>
          I bring strategic thinking on growth, community building, partnerships, and go-to-market. I become a committed partner in the company's trajectory, not a name on a slide deck.
        </p>
        <p>
          If you're building something ambitious in AI and want a thought partner who understands both the technology landscape and the human side of adoption, I'd love to talk.
        </p>
        <CTALink href="#contact">Start a conversation</CTALink>
      </div>
      <div class="service__visual">
        <div class="service__visual-placeholder">
          <span>Photo</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Consulting -->
  <section class="service service--alt" id="consulting">
    <div class="service__inner service__inner--reverse container">
      <div class="service__visual">
        <div class="service__visual-placeholder">
          <span>Photo</span>
        </div>
      </div>
      <div class="service__text">
        <span class="service__label">For Individuals</span>
        <h2 class="service__title">Consulting</h2>
        <p>
          I consult with professionals navigating career transitions into AI, building their public presence, developing strategic clarity, or thinking through written work.
        </p>
        <p>
          These are paid engagements — structured conversations and follow-up designed to get you unstuck and moving with confidence.
        </p>
        <p>
          If you're at an inflection point and want an experienced thought partner who's been in the room where these decisions get made, let's connect.
        </p>
        <CTALink href="#contact">Start a conversation</CTALink>
      </div>
    </div>
  </section>

  <IntakeForm />
</BaseLayout>

<style>
  .page-header {
    padding-top: var(--space-3xl);
    padding-bottom: var(--space-xl);
  }
  .page-header :global(.section-label) {
    margin-bottom: var(--space-md);
  }
  .page-header__title {
    font-family: var(--font-display);
    font-size: clamp(1.8rem, 3vw, 2.5rem);
    font-weight: 300;
    line-height: 1.3;
    letter-spacing: -0.01em;
    color: var(--espresso);
    max-width: 750px;
  }
  .page-header__subtext {
    margin-top: var(--space-md);
    max-width: 550px;
    color: var(--text-secondary);
  }

  .service {
    padding-block: var(--space-3xl);
  }
  .service--alt {
    background-color: var(--bg-warm);
  }
  .service__inner {
    display: grid;
    grid-template-columns: 60% 1fr;
    gap: 5rem;
    align-items: center;
  }
  .service__inner--reverse {
    grid-template-columns: 1fr 60%;
  }
  .service__label {
    font-family: var(--font-body);
    font-size: 0.7rem;
    font-weight: 500;
    letter-spacing: 0.2em;
    text-transform: uppercase;
    color: var(--accent);
    display: block;
    margin-bottom: var(--space-sm);
  }
  .service__title {
    font-size: 1.75rem;
    font-weight: 400;
    margin-bottom: var(--space-md);
  }
  .service__text p {
    color: var(--text-secondary);
    font-size: 0.95rem;
    margin-bottom: var(--space-sm);
  }
  .service__text :global(.cta-link) {
    margin-top: var(--space-sm);
  }
  .service__visual-placeholder {
    aspect-ratio: 4 / 5;
    background: var(--bg-warm);
    border-radius: 2px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-tertiary);
    font-size: 0.85rem;
    letter-spacing: 0.1em;
    text-transform: uppercase;
  }
  .service--alt .service__visual-placeholder {
    background: var(--bg);
  }

  @media (max-width: 768px) {
    .service__inner,
    .service__inner--reverse {
      grid-template-columns: 1fr;
      gap: var(--space-lg);
    }
    .service__visual {
      order: -1;
    }
  }
</style>
